import type { Metadata } from "next";
import "./globals.css";

export const metadata: Metadata = {
  title: "t0.dev",
  description: "Generated by Tarun0x0",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
